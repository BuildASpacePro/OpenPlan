version: '3.8'

services:
  mission-planner:
    build: .
    ports:
      - "4321:4321"
    environment:
      - NODE_ENV=production
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run preview -- --host 0.0.0.0